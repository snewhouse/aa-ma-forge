# sole-dev-merge-pr-workflow Post-Impl Adversarial Review

_Generated by `Skill(verify-impl)` at Phase 6.8 of `/execute-aa-ma-milestone`._

## Milestone 1 — Pre-flight + scope-aware CI checks

- **Audit window:** `e86a733..HEAD` (5 implementation commits: 5914df4, a7a437b, b6342e0, 38b8b8b, dbad361)
- **Audit-Profile:** code-only
- **Dispatched agents:** 5 (full slate per profile spec)
- **Date:** 2026-05-18

### Summary

| Agent | CRITICAL | HIGH | MEDIUM | LOW | Verdict |
|---|:--:|:--:|:--:|:--:|---|
| code-reviewer | 0 | 0 | 0 | 6 | PASS |
| security-auditor | 0 | 0 | 0 | 5 | PASS |
| tdd-sequence-auditor | 1 | 0 | 0 | 0 | FAIL → **DISPUTED** |
| context7-evidence-auditor | 0 | 0 | 0 | 0 | PASS |
| future-proofing-auditor | 0 | 0 | 0 | 3 | PASS |
| **TOTAL** | **1** | 0 | 0 | **14** | **PASS_WITH_WARNINGS** (post-dispute) |

### Overall verdict

**PASS_WITH_WARNINGS** — after user override decision on the single CRITICAL.

The 14 LOW findings are advisory and tracked here for visibility. None block the M1 HARD gate.

---

## User Override Decisions

| # | Severity | Finding | Decision | Recorded |
|---|---|---|---|---|
| 1 | CRITICAL | tdd-sequence FAIL — first src commit (5914df4 @ 16:29:27) precedes first tests commit (dbad361 @ 16:48:22) by ~19min; no `TDD-Waiver:` declared on M1 | **dispute** | 2026-05-18 |

### Dispute rationale (CRITICAL #1)

The audit's verdict is mechanically correct under its narrow git-log lens. However:

1. **Empirical inline validation was performed for every implementation step.** `provenance.log` records explicit pass/fail evidence per Step:
   - Step 1.2: "empirical test 5/5 PASS (on-main rc=1, dirty rc=1, no-remote rc=1, no-commits-ahead rc=1, happy rc=0)"
   - Step 1.3: "empirical canonical AC §4.1.3 test PASS — out-of-scope foo.py zero diff, in-scope new_file.py ruff-clean"
   - Step 1.4: "empirical AC §4.1.4 PASS (dirty→commit with exact subject; clean→no-op)"
   Each was a sandbox-heredoc test run executed BEFORE the implementation commit landed.

2. **The bats tests (Steps 1.5/1.6) formalize that inline coverage** into a maintainable suite. Result: 10/10 bats tests PASS (verified via `bats tests/commands/sole-dev-merge/`).

3. **The plan's HTP explicitly ordered impl-then-tests** (Steps 1.2–1.4 implementation, Steps 1.5–1.6 tests). The git-log order is per-plan, not anti-TDD.

4. **The canonical TDD-Waiver enum does not include "inline-empirical-validated"** (the auditor noted: only `refactor | docs-only | prototype | hotfix-emergency | tooling-config`). Adding a new canonical value is out of M1 scope; tracked as future ADR.

5. **No regression risk:** all empirical evidence was preserved in the bats suite. The audit's concern (untested code shipping) is empirically falsifiable — every line of Stage A and B has both an inline-empirical and a bats assertion.

**Action:** dispute logged here; M1 finalization proceeds.

---

## Low-severity findings (advisory)

### code-reviewer (6 LOW)

1. **mechanism duplication** — `mkcommit` helper duplicated verbatim across `test_stage_a_preflight.bats:36-50` and `test_stage_b_scope.bats:37-51`. M2-M5 will add 3+ more bats files; extract to `fixtures/helpers.bash` later.
2. **DRY** — two near-identical `while read` loops over `$CHANGED_PY` in `sole-dev-merge.md:124-140`; can fuse into one loop running both `ruff format` and `ruff check --fix` per file.
3. **dead code (within milestone)** — `CHANGED_SH`/`N_SH` computed in Stage B but unused; consumed by Stage C4 in M2. Add a `# consumed by Stage C4` comment.
4. **porcelain parsing fragility** — `path="${line:3}"` ignores quoted paths and rename status; silently no-ops via `2>/dev/null`. Use `git status --porcelain -z` with NUL-delimited parsing for robustness.
5. **magic offset** — bare literal `3` for porcelain XY+space prefix; extract to a comment-anchored constant.
6. **KISS test affordance** — defensive `BASE_REF="${BASE_REF:-main}"` fallback in Stage B exists only for standalone bats runs. Document as test affordance or remove.

### security-auditor (5 LOW)

All findings are correctness gaps within the sole-dev trust boundary — no exploitation paths.

1. Porcelain parsing assumes unquoted paths (sole-dev tolerance).
2. Rename rows ("R old -> new") would silently no-op the L-007 guard.
3. `pre-commit run --files $CHANGED_FILES` intentionally word-splits (SC2086 disabled); paths with whitespace get split.
4. TOCTOU between `[[ -f "$f" ]]` and `ruff` invocation — non-exploitable in sole-dev.
5. `extract_stage.sh` passes `$STAGE` to awk `-v`; backslashes in marker name would trigger awk escape interpretation. Developer-controlled input only.

### future-proofing-auditor (3 LOW)

1. `900s` / `15-min poll` value duplicated between Stage G2 prose and exit-status table.
2. Assumptions section says "main is the default branch" but Stage A accepts master too.
3. Tests assert literal `main` in some places vs `$DEFAULT_BRANCH` in code.

---

## Disposition

- **All LOW findings**: tracked in this document; addressable opportunistically during M2–M5 work where the files are revisited.
- **Specifically deferred to M2**: helper extraction (`fixtures/helpers.bash`) when the 3rd bats file lands.
- **No blocking findings** post-dispute.
- **Provenance entry**: `§6.8 POST_IMPL_REVIEW — Audit-Profile: code-only — agents: 5 — verdict: PASS_WITH_WARNINGS — findings: 1 CRITICAL (disputed) / 0 WARNING / 14 LOW`.
