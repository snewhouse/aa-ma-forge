# edge-malformed — intentionally broken

This task file has no milestone headers at all — parser should treat as
ParseError so discover_tasks can attach parse_error and continue.

Just prose. No `## Milestone N:` heading anywhere.
