"""codemem CLI entry point. Placeholder — filled during M1 Step 1.9."""

import sys


def main() -> int:
    print("codemem CLI — not yet implemented (M1 Step 1.9)", file=sys.stderr)
    return 1


if __name__ == "__main__":
    raise SystemExit(main())
