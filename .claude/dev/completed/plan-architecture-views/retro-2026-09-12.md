# Engineering Retrospective — plan-architecture-views

**Window:** 2026-09-11 → 2026-09-12 (2 days; `git log origin/main --since=2026-09-11`)
**Repo:** aa-ma-forge · branch `main` · contributor: Stephen J Newhouse (solo — team breakdown skipped)
**Times:** Europe/London (the author's commit timezone; the skill's Pacific default would put every session on the wrong day)
**Scope note:** the window also contains the close of `milestone-grammar-ssot` M4/M5 (13 commits, 11 Sep evening → 12 Sep 06:20). Metrics below cover the whole window; the plan-specific split is called out where it matters.

## Metrics

| Metric | Value |
|---|---|
| Commits to main | 58 (39 plan-architecture-views · 13 milestone-grammar-ssot · 6 `[ad-hoc]`) |
| Contributors | 1 |
| PRs merged | 0 (sole-dev, direct to main) |
| Insertions / deletions / net | +7 450 / −1 285 / **+6 165** |
| Test LOC (insertions) | 2 624 → **35 %** test ratio |
| Version range | v0.11.0 (tagged 11 Sep morning, before M1) → `## Unreleased` staged for **v0.12.0** |
| Active days | 2 |
| Sessions (45-min gap) | 6 — 3 deep, 0 medium, 3 micro |
| Active coding time | 270 min · **1 657 ins/h** |
| Test health | 99 test files · 21 touched this window · +2 547/−324 LOC under `tests/` |
| Backlog (TODOS.md) | 2 open (both P3, S), 0 completed this window; both were *added* by this plan's reviews |

## Commit time distribution (Europe/London)

```
Hour  Commits
 06:    2  ██
 10:    7  ███████
 11:    2  ██
 12:   16  ████████████████
 13:   10  ██████████
 14:    1  █
 16:    5  █████
 18:    8  ████████
 19:    1  █
 22:    6  ██████
```

Bimodal: a **midday block (10:00–14:06, 35 commits — 60 %)** and an evening tail (18:00–22:10). One late-night micro-session at 22:03 Fri closed grammar-ssot M5. Dead zone 14:06–16:48 and 19:13–22:03.

## Sessions

| Session | Commits | Duration | What |
|---|---|---|---|
| Fri 18:10–19:13 | 9 | 63 min · deep | grammar-ssot M4 close + M5 start |
| Fri 22:03–22:10 | 6 | 7 min · micro | grammar-ssot M5 close-out sync |
| Sat 06:16–06:20 | 2 | 4 min · micro | v0.11.0 bump; plan-architecture-views plan lands |
| Sat 10:06–11:11 | 9 | 66 min · deep | **M1 Standard** (13 elements, ADR-0010, Angle 6 #6/#7) |
| Sat 12:06–14:06 | 27 | 121 min · deep | **M2 Lint + M3 Share** — the plan's core; 27 commits in 2 h |
| Sat 16:48–16:58 | 5 | 10 min · micro | **M4 Render** close, archive, L-014 |

M4's real work (14:06–16:48) shows as a "dead zone" because its commits are batched at the end — 9 commits in the window but the prototype, two review rounds (8 agents) and the SRI/CSP rework all landed inside `887a571`/`564f993`. Session detection under-reads AFK-heavy milestones.

## Commit types

```
feat:     17  (29%)  █████████████████
docs:     15  (25%)  ███████████████
chore:    10  (17%)  ██████████
fix:       7  (12%)  ███████
test:      7  (12%)  ███████
refactor:  1  ( 1%)  █
bump:      1  ( 1%)  █
```

Fix ratio 12 % — well under the 50 % "ship fast, fix fast" flag. Every `fix(render)` commit is a review-driven hardening with a test (`%%` directives / shape labels / unterminated fences; ReDoS; stem collision; SRI+CSP), not a regression repair. `test:` commits precede their `feat:` in every milestone (TDD auditor PASS ×2).

## Hotspots

| Changes | File |
|---|---|
| 31 | `…/plan-architecture-views-tasks.md` |
| 16 | `…/plan-architecture-views-provenance.log` |
| 12 | `…/plan-architecture-views-context-log.md` |
| 12 | `…/milestone-grammar-ssot-provenance.log` |
| 10 | `…/milestone-grammar-ssot-tasks.md` |
| 9 | `…/plan-architecture-views-reference.md` |
| 7 | `docs/adr/0010-architecture-views-and-render.md` |
| 6 | `pyproject.toml`, `README.md`, `CHANGELOG.md` |
| 5 | `tests/test_gate.py`, `claude-code/skills/plan-verification/SKILL.md` |

Churn is where it should be: AA-MA artifacts (sub-step sync rule) and the ADR that this plan made its own exemplar. `pyproject.toml` ×6 is the honest cost of two new CLIs + one dep promotion. No production file is a churn hotspot — `mermaid_lint.py` and `html.py` each landed GREEN once and were then hardened by named review findings.

## Size distribution

S 38 · M 15 · L 4 · **XL 1** — the XL is the plan document itself (`8c632bc`, 1 667 lines, 7 files). Largest code commits: `38dfc82` gate-reads-SSoT (757, grammar-ssot), `73232c3` four §6.8 false-PASS fixes (674), `542789b` lint RED fixtures (383, 23 fixture files).

## Focus score

93 % of commits touch `.claude/dev/` (sync discipline). By code area: `tests/` 26 commits, `docs/` 17, `src/` 16, `claude-code/` 11 — a test-first, doc-heavy plan by design (M1 and M3 were docs-only).

## Ship of the window

**M2 Lint — `aa-ma-lint-views`** (`542789b` → `d353220` → `53ed877` → `0bd8987`, ~1 000 LOC). A pure-Python §13 lint reusing `grammar` + `plan_parsers`, 10 finding codes, `mmdc` render seam that is `UNKNOWN`-not-`PASS` when the tool is absent (L-012), leaf-package import contract, and a security review that reproduced two ReDoS (200 KB → 14–20 s) which became linear scanners (≤0.04 s). It is now the check that Angle 6 #6 runs on every new plan.

Runner-up: **M4 Render's SRI turn** — the security auditor measured that SRI on the mermaid ESM entry cannot attest its lazily imported chunks; the shipped page loads the single-file UMD bundle with `integrity=` behind a CSP, and the negative control (tampered hash → browser refuses the script) is in provenance.

## Your patterns

- **Peak:** 12:00–14:00. Your three deep sessions produced 45 of 58 commits.
- **Biggest ship:** M2 Lint (above).
- **Test discipline:** 35 % test LOC; every code milestone RED→GREEN with the RED committed first; 8 review agents across M2–M4 returned 5 CRITICAL + 12 WARNING and **all 17 were fixed with a test before the gate** (L-013 held for the whole plan — zero "acknowledged").
- **Praise:** the M3 exemplar publish was verified from the *user's saved viewer document* when `/browse` lost the session twice — evidence was measured, not inferred, even when the tooling fought back. The M4 decision (build vs drop) was deferred to the gate with the checkpoint spelling out both paths; when built, the prototype ran before any production line.
- **Opportunity:** two of the three §6.8 CRITICALs in this plan were self-inflicted process slips, not code — a `ruff format` sweep that re-wrapped lines outside the milestone (L-007) and a `git mv` that dropped freshly written headers (now L-014). Both were caught by reading `git status`/`git diff` before reporting done. A pre-commit habit of `git diff --stat <base>..HEAD -- tests/` (files touched outside the declared artefacts) would catch the first class before an agent has to.

## Streak

- Team/your shipping streak: **2 consecutive days** (11–12 Sep). Prior activity cluster 9–11 Aug; the repo ships in bursts around plans, not daily.

## Trends vs last retro (2026-05-08, 7d window)

```
                    Last        Now         Delta
Commits:            16     →    58          ↑263 %  (2-day window vs 7-day)
Net LOC:            4 643  →    6 165       ↑33 %
Test ratio:         38 %   →    35 %        ↓3 pp   (M1/M3 docs-only milestones dilute it)
Sessions:           5      →    6           ↑1
Deep sessions:      1      →    3           ↑2
Avg session:        19 min →    45 min      ↑26 min
LOC/session-hour:   2 850  →    1 657       ↓42 %   (review-driven hardening, not bulk scaffolding)
Fix ratio:          6 %    →    12 %        ↑6 pp   (all review-driven, all tested)
Chore share:        50 %   →    17 %        ↓33 pp
```

## Actions

1. `cz bump` → **v0.12.0** (CHANGELOG `## Unreleased` has 9 Added bullets; L-003 — never edit version headings).
2. TODOS.md: both P3 items (`aa-ma-tui --graph`, TUI runnable from any project) are now unblocked — their dependencies (grammar-ssot M5, share M3) shipped this window.
3. Consider a `--since <ISO-date>` form in the retro skill's validator: `/archive-aa-ma` Step 6 passes exactly that and the skill's usage text rejects it.
