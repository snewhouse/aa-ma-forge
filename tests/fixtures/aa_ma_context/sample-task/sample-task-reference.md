# sample-task — Reference

## Core API

Key function: `load_config` in `packages/sample/src/config.py`.

Related helper: `parse_yaml` exposed by `packages/sample/src/loader.py`.

Bare words like `True`, `None`, `string` must NOT be treated as symbols.
