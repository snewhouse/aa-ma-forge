"""Snapshot fixture apps for pytest-textual-snapshot.

Each file in this package exposes a module-level `app = AAMAApp(...)`
that pytest-textual-snapshot's `snap_compare` fixture can run and
capture as an SVG.
"""
