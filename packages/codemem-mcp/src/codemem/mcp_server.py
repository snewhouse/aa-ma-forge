"""codemem MCP server entry point. Placeholder — filled during M1 Step 1.10."""

import sys


def serve() -> int:
    print("codemem MCP server — not yet implemented (M1 Step 1.10)", file=sys.stderr)
    return 1


if __name__ == "__main__":
    raise SystemExit(serve())
