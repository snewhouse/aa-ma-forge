# Reference — Fix Pre-existing Drift & Release v0.9.0

**Last Updated:** 2026-05-13

## Immutable Facts

### Repo state (verified at plan time)

- **Repo root:** `/home/sjnewhouse/biorelate/projects/gitlab/github_private/aa-ma-forge`
- **Current branch:** `feature/understand-codebase-skill`
- **Current HEAD:** `7cacff5f0379b3c19b9f7a3f10c24dd3e73933b6` (commit `7cacff5 feat(skills): vendor understand-codebase skill + 4 onboarding agents + /understand-codebase command`)
- **`origin/main`:** `3a903259a5852272d1f38ed60588e5ab2cddfbad` (`3a90325`)
- **Ahead of main by:** 1 commit (`7cacff5`)
- **Remote:** GitHub-only — `https://github.com/snewhouse/aa-ma-forge.git` (`origin` fetch + push). No GitLab remote.
- **Last release tag:** `v0.8.0`
- **Current declared version:** `0.8.0` (in `pyproject.toml` and `VERSION`)
- **Working tree:** 2 modified files (auto-generated by `pre-compact-aa-ma.sh` hook at `2026-05-13T09:26:58Z`) — see "Dirty Working Tree Facts" below.

### `cz bump` config (in `pyproject.toml`)

```toml
[tool.commitizen]
name = "cz_conventional_commits"
version_provider = "pep621"
tag_format = "v$version"
update_changelog_on_bump = true
version_files = [
    "VERSION:__version__",
]

[tool.semantic_release]
major_on_zero = false
tag_format = "v{version}"
commit_message = "chore(release): v{version}"
```

### Deterministic version computation

- Commits since `v0.8.0`: **1** (`7cacff5 feat(skills): …`).
- `feat:` commits trigger **minor bump** under `cz_conventional_commits` defaults.
- `major_on_zero = false` prevents jumping from `0.x.y` → `1.0.0` on the first breaking change (irrelevant here — no breaking change in pipeline).
- **Computed next version: `0.9.0`.**
- **Computed next tag: `v0.9.0`** (per `tag_format = "v$version"`).

### Pre-existing drift inventory (verified)

**Source of truth for counts:**

| Asset class | Count | Verified by |
|---|---|---|
| Hooks (`*.sh` directly under `claude-code/hooks/`, excluding `lib/`) | **8** | `ls claude-code/hooks/*.sh \| wc -l` = 8 |
| Skills (`claude-code/skills/*/`) | **18** | `ls -d claude-code/skills/*/ \| wc -l` = 18 |
| Commands (`claude-code/commands/*.md`) | **10** | `ls claude-code/commands/*.md \| wc -l` = 10 |
| Agents (`claude-code/agents/*.md`) | **11** | `ls claude-code/agents/*.md \| wc -l` = 11 |
| AA-MA task file types (`docs/templates/*-template.md` + `*.yaml`) | **8** | 5 standard (plan, reference, context-log, tasks, provenance) + 3 optional (impl-review, tests, verification). Engineering-standards-template is a non-task content template, not counted. |

**Stale references found in `README.md` (the drift items to fix):**

1. `README.md:37` — `docs/templates/     Ready-to-use templates for all 7 AA-MA file types` → should read `… all 8 AA-MA file types (5 standard + 3 optional)`.
2. `README.md:307` — `AA-MA Forge ships five hooks. All honour a single master kill switch …` → should read `AA-MA Forge ships eight hooks. …`.
3. `README.md:222-238` — Skills table contains **13** rows; actual skill count is 18. Missing 5 (alphabetical install order):
   - `grill-with-docs`
   - `prototype`
   - `understand-codebase`
   - `verify-impl`
   - `write-a-skill`

**Skill descriptions (sourced verbatim from each `claude-code/skills/<name>/SKILL.md` frontmatter `description:`):**

- `grill-with-docs` — Interview-style plan grilling that lazily writes `CONTEXT.md` and ADRs as decisions crystallise (forked from gstack; auto-invoked by `/aa-ma-plan` Phase 1.3 in `with-docs` mode).
- `prototype` — Branches between LOGIC (terminal TUI for state/business-logic questions) and UI (web-frontend `?variant=` swap) prototypes; gates Theme-1 `Prototype-Required: YES` evidence.
- `understand-codebase` — Tiered onboarding for a new/inherited/shared codebase; produces `ONBOARDING.md` + `.claude/onboarding/` deep-dives; Deep tier runs a TeamCreate agent-team.
- `verify-impl` — Post-impl adversarial review symmetric to `/verify-plan`; dispatches up to 5 parallel audit agents per milestone `Audit-Profile`; CRITICAL findings gate user authorisation (Phase 6.8).
- `write-a-skill` — Authoring template + checklist for new skills (frontmatter contract, references/ layout, install.sh auto-discovery requirements).

**Note:** the README Skills table is presently a non-exhaustive curated subset and is missing OTHER skills too (`debugging-strategies`, `defense-in-depth`, `dispatching-parallel-agents`, `token-compression`, `complexity-router`, `agent-teams`, `retro` are already in it). Final table should contain **all 18** entries in alphabetical order — i.e. we **append the 5 missing**, not rewrite the whole table.

### Dirty working tree facts

The two unstaged files at plan time are the `pre-compact-aa-ma.sh` hook's checkpoint output. Both belong to the (now-complete) `understand-codebase-skill` task. Diff (verbatim):

`.claude/dev/active/understand-codebase-skill/understand-codebase-skill-context-log.md` (+5 lines):
```
## [2026-05-13T09:26:58Z] Compaction Summary (auto-generated by hook)
- Active step at compaction: Sub-step 1.6: Verify symlinks + backups
- Snapshot saved to: /home/sjnewhouse/.claude/hooks/cache/compaction-snapshots/understand-codebase-skill-snapshot.md
- Note: Context compacted. Reload AA-MA files to resume.
```

`.claude/dev/active/understand-codebase-skill/understand-codebase-skill-provenance.log` (+2 lines):
```
[2026-05-13T09:26:58Z] Context compacted — Snapshot saved, active step: Sub-step 1.6: Verify symlinks + backups
[2026-05-13T09:26:58Z] CHECKPOINT — ActiveStep: Sub-step 1.6: Verify symlinks + backups — NextAction: "Resume from active step" — ContextLoaded: REFERENCE,TASKS — TokenUsage: N/A
```

### Historical CHANGELOG note (DO NOT touch)

`CHANGELOG.md:479` contains the line `Standalone templates for all 7 AA-MA files in docs/templates/ …`. This is a **v0.6.0 historical entry** and per `CLAUDE.md` doctrine ("Historical docs are frozen — Files in `docs/plans/` reflect what was planned at the time; don't retroactively update them") it is left as-is. Only the `README.md:37` reference is updated.

### Relevant commands + hooks (paths)

- `/sole-dev-merge` → `claude-code/commands/sole-dev-merge.md` (407 lines; ff-only merge to main + return to feature)
- `/archive-aa-ma` → `claude-code/commands/archive-aa-ma.md` (214 lines; mv active → completed)
- `aa-ma-commit-signature.sh` BLOCKING PreToolUse hook — requires `[AA-MA Plan] <task> .claude/dev/active/<task>` footer when active. `[ad-hoc]` bypass for plan-unrelated commits.
- `aa-ma-commit-drift.sh` post-commit hook — advisory only (exits 0). Warns if commit doesn't touch `tasks.md`/`provenance.log`.

### CHANGELOG `## [Unreleased]` (what `cz bump` will promote to `v0.9.0`)

Already populated at plan time with `### Feat` (understand-codebase vendoring) + `### Docs` (foundations reconcile + count bumps). `cz bump` rewrites the section header from `## [Unreleased]` → `## v0.9.0 (2026-05-13)` per `update_changelog_on_bump = true`.

### Verified pre-flight commands

- `git status --short` → 2 modified files (as documented above).
- `git log --oneline v0.8.0..HEAD` → `7cacff5 feat(skills): vendor …` (single commit).
- `ls claude-code/hooks/*.sh | wc -l` → `8`.
- `ls -d claude-code/skills/*/ | wc -l` → `18`.
- `ls docs/templates/*-template.* 2>/dev/null | wc -l` → 9 (includes `engineering-standards-template.md`; subtract 1 for the non-task template → 8 AA-MA task file types).
- `grep -m1 '^version' pyproject.toml` → `version = "0.8.0"`.
- `cat VERSION` → `__version__ = "0.8.0"`.
